#pragma once
#include <QDir>
#include <QString>

/// The following function should be used to get the direct filepath
/// to this Qt project so that you can read the GLSL files used for
/// your shader (from hw05)
QString getCurrentPath();
